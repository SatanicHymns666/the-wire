# Claude Instructions

## Git workflow

Always commit and push directly to `main`. Do not create feature branches. Do not open pull requests. If the session environment automatically creates a feature branch, push to `main` regardless using `git push origin HEAD:main` or `git push origin <branch>:main`.
